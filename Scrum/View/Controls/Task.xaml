<UserControl x:Class="Scrum.View.Controls.Task"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Scrum.View"
            mc:Ignorable="d" 
             d:DesignHeight="180" d:DesignWidth="320" DataContext="User" >
    <Grid Background="OldLace">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="3" Grid.Row="0">
            <TextBlock Text="{Binding Assignee.UserId }" FontWeight="Bold"/>
            <TextBlock Text="{Binding RankNumber}" HorizontalAlignment="Right" VerticalAlignment="Top"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="3" Grid.Row="1">
            <TextBlock Text="{Binding Title}"/>
            <TextBlock Text="{Binding RankNumber}" HorizontalAlignment="Right" VerticalAlignment="Top"/>
        </StackPanel>
        <StackPanel Background="SlateGray" Orientation="Horizontal" Grid.Row="2">
            <TextBox Text="{Binding TicketNumber}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
        </StackPanel>
    </Grid>
</UserControl>
